<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="galaxy.app.stressdetector">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature android:name="android.hardware.camera.flash" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StressDetector">
        <activity android:name=".familyList" />
        <activity android:name=".StressQuest" />
        <activity android:name=".UsageInstruction" />
        <activity android:name=".ThankYouActivity" />
        <activity android:name=".pam" />
        <activity android:name=".Survey" />
        <activity android:name=".AdviceOptionActivity" />
        <activity android:name=".AdviceResultActivity" />
        <activity android:name=".Advice" />
        <activity android:name=".Title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Results" />
        <activity android:name=".HistoryResult" />
        <activity android:name=".History" />
        <activity android:name=".Camera" />
        <activity android:name=".Instruction" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>